"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/UploadFile');
const Fixture = require('../../fixtures/UploadFileFixture');

describe('models:UploadFile', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
