"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Template');
const Fixture = require('../../fixtures/TemplateFixture');

describe('models:Template', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
