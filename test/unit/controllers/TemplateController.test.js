"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/TemplateController');

describe('controllers:TemplateController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
