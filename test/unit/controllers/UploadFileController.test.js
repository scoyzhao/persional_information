"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/UploadFileController');

describe('controllers:UploadFileController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
